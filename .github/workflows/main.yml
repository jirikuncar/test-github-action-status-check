name: CI

on:
  push:
    paths-ignore:
    - README.md

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Good to go
      if: hashFile('README.md')
      run: echo OK
    - name: I am documentation
      if: ! hashFiles('README.md')
      run: echo README
